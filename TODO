// in $$ it returns current pid, for that we are using get_pid which is not allowed, it need to be chenged to fork and return child id - 1

// change char counts in write functions

// PONCHIKI_MINISHELL:  echo "ughijokp
// No closing qoute
// minishell(56197,0x1166035c0) malloc: *** error for object 0x7fff741e1196: pointer being freed was not allocated
// minishell(56197,0x1166035c0) malloc: *** set a breakpoint in malloc_error_break to debug

// cd .. | pwd

// validation for export                   DONE(ls || (ls -la && ls || ls)) && ls

// shlvl

// in dollar name is as long as it is underline number or alphabetic char also in export

// in export not sort the list just print sorted list

// heredoc i double free